# API Integration Documentation

## Обзор изменений

Проект полностью интегрирован с backend API согласно спецификации из `swagger.json`.

## Ключевые особенности

### 🔐 Автоматическое обновление токенов

- **Access Token**: живет 15 минут
- **Refresh Token**: живет 1 час

При истечении access token:
1. Автоматически вызывается `/api/auth/refresh`
2. Получаются новые токены
3. Исходный запрос повторяется с новым токеном

При истечении refresh token:
1. Токены очищаются из localStorage
2. Пользователь перенаправляется на страницу авторизации

### 🔄 Функция `fetchWithAuth()`

Все защищенные эндпоинты используют `fetchWithAuth()`, которая:
- Автоматически добавляет Authorization header
- Перехватывает ошибки 401
- Обновляет токены при необходимости
- Повторяет запрос с новым токеном

### 📝 Реализованные эндпоинты

#### Аутентификация
- ✅ `POST /api/auth/register` - регистрация пользователя
- ✅ `POST /api/auth/login` - авторизация
- ✅ `POST /api/auth/refresh` - обновление токенов (автоматически)
- ✅ `POST /api/auth/logout` - выход из системы

#### Диалоги
- ✅ `POST /api/dialogs/with-files` - создать диалог с файлами
- ✅ `POST /api/dialogs/{dialogId}/files` - загрузить файлы в диалог
- ✅ `GET /api/dialogs/{dialogId}/files` - получить список файлов
- ✅ `POST /api/dialogs/{dialogId}/send-question` - отправить сообщение
- ✅ `GET /api/dialogs/{dialogId}/messages` - получить историю сообщений
- ✅ `GET /api/dialogs` - получить список всех диалогов
- ✅ `DELETE /api/dialogs/{dialogId}` - удалить диалог

## Структура проекта

### API Service (`src/services/api.ts`)

```typescript
// Типы из swagger.json
export type AuthResponse = {...}
export type DialogResponse = {...}
export type FileResponse = {...}
export type MessageResponse = {...}

// Функции управления токенами
export function getAccessToken(): string | null
export function saveTokens(accessToken, refreshToken)
export function clearTokens()
export function isAuthenticated(): boolean

// API функции
export async function register(userName, email, password)
export async function login(userName, password)
export async function logout()

export async function createDialogWithFiles(files: File[])
export async function uploadFilesToDialog(dialogId, files)
export async function getDialogFiles(dialogId)
export async function sendMessage(dialogId, question)
export async function getDialogMessages(dialogId)
export async function getDialogs()
export async function deleteDialog(dialogId)
```

### Компоненты

#### App.tsx
- Проверяет наличие токенов при загрузке
- Управляет состоянием аутентификации
- Настраивает маршруты

#### PrivateRoute.tsx
- Защищает приватные маршруты
- Перенаправляет неавторизованных пользователей на `/auth`

#### UploadPage.tsx
Полнофункциональный интерфейс для работы с диалогами:
- Список всех диалогов пользователя
- Создание нового диалога с файлами
- Загрузка дополнительных файлов в существующий диалог
- Отправка сообщений и получение ответов от AI
- Просмотр истории сообщений
- Удаление диалогов

## Обработка ошибок

### Ошибки авторизации (401)
- Автоматически пытается обновить токен
- Если refresh token истек → редирект на `/auth`

### Ошибки бизнес-логики (400, 404, etc.)
- Отображаются пользователю в виде сообщений
- Парсятся из ErrorResponse согласно swagger

### Сетевые ошибки
- Отображаются generic сообщения об ошибке

## Безопасность

### Хранение токенов
- Токены хранятся в `localStorage`
- Ключи: `accessToken`, `refreshToken`

### Authorization Header
```
Authorization: Bearer <accessToken>
```

### Автоматическое обновление
- Механизм предотвращает множественные одновременные обновления токена
- Использует паттерн подписки для ожидающих запросов

## Пример использования

### Отправка сообщения
```typescript
import * as api from './services/api';

const response = await api.sendMessage(dialogId, "Как решить эту задачу?");
console.log(response.answer); // Ответ от AI
```

### Создание диалога с файлами
```typescript
const files = [file1, file2];
const dialog = await api.createDialogWithFiles(files);
console.log(dialog.dialogId); // ID созданного диалога
```

### Обработка автоматического обновления токена
```typescript
// Не нужно ничего делать вручную!
// fetchWithAuth() автоматически обновит токен при необходимости
const dialogs = await api.getDialogs();
```

## Тестирование

### Локальный запуск
1. Убедитесь, что backend работает на `http://localhost:8080`
2. Запустите frontend: `npm start`
3. Откройте `http://localhost:3000`

### Проверка автоматического обновления токенов
1. Авторизуйтесь
2. Подождите 15 минут (или измените время жизни токена на backend)
3. Попробуйте отправить сообщение
4. Токен должен автоматически обновиться, запрос выполнится

### Проверка истечения refresh token
1. Авторизуйтесь
2. Подождите 1 час (или измените время жизни refresh token)
3. Попробуйте выполнить действие
4. Должен произойти автоматический редирект на `/auth`

## Изменения в коде

### Удалено
- ❌ `GET /api/users/me` - эндпоинт отсутствует в swagger
- ❌ `POST /api/files/upload` - заменен на API диалогов
- ❌ `src/types/user.ts` - больше не используется

### Изменено
- ✅ Авторизация теперь проверяет наличие токенов, а не запрашивает `/me`
- ✅ Загрузка файлов работает через создание/обновление диалогов
- ✅ Добавлена полная поддержка работы с диалогами

### Добавлено
- ✅ Автоматическое обновление токенов
- ✅ Обработка истечения refresh token
- ✅ API для работы с диалогами
- ✅ Интерфейс чата с AI
- ✅ Управление диалогами (создание, просмотр, удаление)

## Переменные окружения

По умолчанию используется:
```typescript
const BASE = "http://localhost:8080";
```

Для production можно изменить на:
```typescript
const BASE = process.env.REACT_APP_API_URL || "http://localhost:8080";
```

И добавить в `.env`:
```
REACT_APP_API_URL=https://api.example.com
```

